# Frontend Source of Truth

The canonical source for the NA Room frontend is **this directory** (`naroom/frontend/`).

The `/Users/dmitrijulybin/Projects/shelter/frontend2/` directory is a scratch workspace.
Any changes made there MUST be reconciled here before they take effect in production.

Build for production: `cd frontend && npm run build`
Run checks: `cd frontend && npm run check`
